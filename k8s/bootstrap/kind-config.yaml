kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: l3bgp-cluster
nodes:
  - role: control-plane
  - role: worker
    labels:
        fabric: leaf1
  - role: worker
    labels:
        fabric: leaf2
networking:
  # the default CNI will not be installed
  disableDefaultCNI: true
